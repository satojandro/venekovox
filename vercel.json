{
  "framework": "vite",
  "buildCommand": "pnpm install --frozen-lockfile && cd apps/front-end && pnpm build",
  "outputDirectory": "apps/front-end/dist",
  "installCommand": "echo 'handled in buildCommand'"
}
